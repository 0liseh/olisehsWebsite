import { NavBarContainer } from "../../styles/NavBar/NavBar.styles"

export const NavBar = (): JSX.Element => {
    return(
        <NavBarContainer>
            This is the NavBar
        </NavBarContainer>
    )
}
